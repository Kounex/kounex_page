import 'package:flutter/material.dart';

import '../../../shared/base_widgets/k_scaffold.dart';

class ImprintDetailView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return KScaffold(
      content: [
        Text('Detail'),
      ],
    );
  }
}
